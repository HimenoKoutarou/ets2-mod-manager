# ETS2 模组管理器 — 开发计划

> 项目目录：F:\ETS2ModManager
> 目标应用：Euro Truck Simulator 2 (ETS2) 模组资源管理器
> 核心功能：批量启用/禁用、拖拽优先级、模组预览图/描述/版本展示

---

## 一、关键背景信息（已调研确认）

### 1.1 ETS2 文件系统

| 文件/目录 | 位置 | 作用 |
|---|---|---|
| mods_info.sii | Documents/Euro Truck Simulator 2/mods_info.sii | 全局模组索引，info[] 数组记录所有被游戏扫描到的模组（格式："模组名|时间戳"） |
| profile.sii | Documents/Euro Truck Simulator 2/profiles/<哈希ID>/profile.sii | 每个存档的配置文件，active_mods[] 数组记录已启用模组及其顺序（数组顺序 = 加载优先级顺序） |
| /mod 文件夹 | Documents/Euro Truck Simulator 2/mod/ | 手动安装的本地模组（.scs / .zip / 文件夹） |
| Workshop 目录 | Steam/steamapps/workshop/content/227300/<WorkshopID>/ | Steam 创意工坊订阅的模组（ETS2 AppID = 227300） |
| Steam Cloud 存档 | Steam/userdata/<用户ID>/227300/remote/profiles/ | 云同步的 profile.sii（结构相同，路径不同） |

### 1.2 模组包内部结构（.scs = zip 格式）

每个模组（.scs/.zip/目录）根目录下可选包含：

| 文件名 | 格式 | 说明 |
|---|---|---|
| manifest.sii | SiiNunit 文本 | 元数据定义：display_name / package_version / author / category[] / icon / description_file / compatible_versions[] / dlc_dependencies[] |
| mod_icon.jpg | JPG 276x162px | 预览图，文件名来自 icon 字段 |
| mod_description.txt | UTF-8 文本 | 详细描述，支持 [red][green][blue][normal][white][orange] 颜色标签 |

### 1.3 优先级（加载顺序）规则

- 列表越靠上 = 优先级越高（同名文件会覆盖下方模组）
- 社区共识的推荐加载顺序（从上到下）：
  1. 经济/游戏数据 Mod
  2. 物理 Mod
  3. UI / HUD Mod
  4. 图形/天气/光照 Mod
  5. 声音 Mod
  6. AI 交通 Mod
  7. 货物 Mod
  8. 涂装 Mod
  9. 挂车 Mod
  10. 卡车 Mod
  11. 地图 Mod（及各种地图修复补丁，需严格按作者说明顺序）

### 1.4 profile.sii 的加密问题

- 本地 profile 默认是加密二进制格式，不能直接文本编辑
- 社区工具 SII_Decrypt（GitHub 可搜）可将其解密/加密
- MVP 阶段策略：先只支持 mods_info.sii 的读取 + 在应用内维护"启用列表"，导出/应用时提示用户需要配合解密，或直接捆绑调用 SII_Decrypt.exe

---

## 二、技术栈选型

### 最终方案：Python 3.11 + PySide6 (Qt 6)

| 维度 | 选择 | 理由 |
|---|---|---|
| 语言 | Python 3.11 | Zip/SCS 解析、SII 文本处理、图片预览实现最简洁 |
| GUI 框架 | PySide6 (LGPL) | QListWidget 拖拽排序开箱即用；Qt Designer 快速搭界面 |
| 打包 | PyInstaller | 单文件夹发布，玩家无需 Python 环境 |
| SCS 解析 | zipfile（标准库）+ 手写 SII 解析 | .scs 就是标准 zip，SiiNunit 语法非常简单 |
| 图片预览 | QPixmap + PIL（Pillow） | 直接加载 jpg，无需额外库 |
| 配置存储 | JSON（settings.json / presets.json） | 保存用户的模组预设方案（批量启用模板） |

### 目录结构（建议）

F:\ETS2ModManager\
+-- src\
|   +-- main.py                  # 程序入口
|   +-- core\
|   |   +-- sii_parser.py        # manifest.sii / mods_info.sii 解析器
|   |   +-- scs_archive.py       # .scs/.zip 读取 + 提取 icon/description
|   |   +-- profile_io.py        # profile.sii 读写 (调用 SII_Decrypt)
|   |   +-- mod_scanner.py       # 扫描 /mod + Workshop 目录
|   |   \-- models.py            # dataclass: Mod, ModCategory, Profile
|   +-- services\
|   |   +-- mod_service.py       # 模组业务逻辑 (筛选/搜索/比对)
|   |   +-- profile_service.py   # 存档模组列表读写
|   |   +-- priority_service.py  # 排序/预设/推荐顺序
|   |   \-- backup_service.py    # 自动备份 profile.sii
|   +-- ui\
|   |   +-- main_window.py       # 主窗口
|   |   +-- mod_list_widget.py   # 模组列表（拖拽排序+复选框）
|   |   +-- mod_detail_panel.py  # 右侧详情面板
|   |   +-- profile_selector.py  # 顶部 Profile 切换栏
|   |   \-- batch_toolbar.py     # 批量操作工具栏
|   \-- utils\
|       +-- paths.py             # ETS2 路径检测
|       \-- sii_crypto.py        # 调用 SII_Decrypt.exe 包装
+-- assets\
|   +-- icons\                   # 应用图标
|   \-- bin\
|       \-- SII_Decrypt.exe      # （可选，捆绑解密工具）
+-- tests\                       # 解析器单元测试
+-- requirements.txt
+-- build.bat                    # PyInstaller 打包脚本
\-- README.md

---

## 三、分阶段开发计划

### 阶段 0：环境搭建（半天）

- [ ] 安装 Python 3.11，创建虚拟环境 venv
- [ ] pip install PySide6 Pillow pyinstaller
- [ ] 按上方目录结构创建骨架空文件
- [ ] 编写 requirements.txt

### 阶段 1：核心解析层（1~2 天）—— 先跑通数据层

目标：脱离 UI，用脚本就能打印出模组列表 + 详情

1. SII 语法解析器 (core/sii_parser.py)
   - 解析 SiiNunit 块格式：{ key:value, key[]:"arr1", key[]:"arr2" }
   - 支持 # 注释、字符串引号、数组展开
   - 单元测试：用你本机的 mods_info.sii 做样本 = 解析 573 条不报错
   - 单元测试：解析一个标准 manifest.sii 样本

2. SCS/zip 包读取 (core/scs_archive.py)
   - 输入：.scs 文件路径 / 目录路径
   - 输出：ModManifest 对象（display_name, version, author, category[], icon_bytes, description_text, compatible_versions[]）
   - 提取 icon 为内存字节流（不落地写文件）
   - 处理 "无 manifest" 情况：回退用文件名做 display_name

3. 模组目录扫描 (core/mod_scanner.py)
   - 自动检测 ETS2 文档路径、Steam Workshop 路径
   - 扫描 /mod 下的 .scs/.zip/目录，每个包生成一个 Mod 对象
   - 结合 mods_info.sii 中的时间戳判断模组是否被游戏识别

### 阶段 2：业务服务层（1 天）

1. ProfileService (services/profile_service.py)
   - 列出所有本地 profile（目录名 + 尝试读取显示名称）
   - 调用 SII_Decrypt.exe 解密 profile.sii，解析出 active_mods[] 数组
   - 写入时：重新按顺序生成 active_mods[]，提示用户需要关闭游戏
   - 写入前自动备份（加时间戳复制到 backup/ 目录）

2. PriorityService（排序服务）
   - move_up(mod_id) / move_down(mod_id) / move_to_index(mod_id, idx)
   - enable_batch([mod_ids]) / disable_all() / enable_all()
   - 预设保存：把当前启用+顺序存为 JSON 预设文件（如 "长途运输方案.json"），可一键加载
   - 推荐顺序：按 category 自动套用社区共识排序（可选功能，MVP 可不做）

3. BackupService
   - 每次写入 profile 前复制原文件
   - 保留最近 10 份历史版本，超过自动清旧

### 阶段 3：UI 层（2~3 天）

#### 主窗口布局（参考游戏内 Mod Manager 风格）

 [Profile: 存档1]           [批量全启] [批量全禁] [保存预设]

 模组1 (地图)       |   预览图 276x162
 模组2 (图形)  ^    |
 模组3 (卡车)  v    |   名称: ProMods Europe v2.82
 模组4 (禁用)       |   作者: ProMods Team
 模组5 (禁用)       |   版本: 2.82
 模组6 (声音)       |   分类: [Map] [Assets]
 ...                |   兼容: 1.57.*  1.58.*
                    |   依赖 DLC: Going East!
  拖拽排序 / 勾选启禁|   描述: ProMods 是最受欢迎的免费...

关键 UI 组件实现要点：

1. ModListWidget（左栏）：
   - 继承 QListWidget，开启 setDragDropMode(InternalMove) = 自动支持拖拽重排
   - 每一行自定义 ItemWidget：[QCheckBox] + [QLabel 图标缩略] + [QLabel 名称] + [QTag 分类]
   - 右键菜单：启用/禁用、上移、下移、置顶、置底

2. ModDetailPanel（右栏）：
   - QLabel 展示预览图（缩放居中）
   - 支持颜色标签的描述显示：自定义 TextBrowser/Label 解析 [red]...[normal] 转 HTML span

3. BatchToolbar（顶部批量操作）：
   - 按钮：全选启用、全部禁用、反选启用、选中置顶、选中置底
   - 预设下拉框：保存当前方案、加载预设、删除预设
   - "写入游戏存档"按钮 = 确认弹窗 = 调用 ProfileService = 备份 = 写入 = 成功提示

4. ProfileSelector：
   - QComboBox 列出所有 profile，切换时重新加载对应 active_mods[]

### 阶段 4：集成测试 & 打包（1 天）

1. 用你自己的存档做真实测试：
   - 先备份一份 profile.sii
   - 启动程序 = 能看到所有 573 个模组 = 预览图显示 = 描述正确
   - 拖几个模组改变顺序 = 写入 = 启动游戏验证顺序是否生效
   - 存几个预设 = 加载 = 切换 profile

2. 异常处理：
   - profile.sii 加密失败：提示"请手动复制 SII_Decrypt.exe 到 /bin 目录"
   - 游戏正在运行：检测 ETS2.exe 进程，阻止写入
   - manifest.sii 解析失败：不崩溃，fallback 显示文件名

3. PyInstaller 打包：
   pyinstaller --name ETS2ModManager --windowed --add-data "assets;assets" src/main.py
   - 产出 dist/ETS2ModManager/ 文件夹，压缩发给朋友即可用

---

## 四、SII Parser 实现伪代码（先行参考）

# core/sii_parser.py  —  SiiNunit 极简解析器
import re
from dataclasses import dataclass, field

@dataclass
class SiiUnit:
    unit_type: str          # e.g. "mod_package"
    unit_name: str          # e.g. ".package_name"
    attrs: dict = field(default_factory=dict)
    # key -> str  or  key -> list[str]

def parse_sii(text: str) -&gt; list[SiiUnit]:
    """
    1. 去掉 # 注释
    2. 正则扫出所有 unit 块
    3. 每个块内逐行解析 key: value / key[]: value
    """
    text = re.sub(r'#.*', '', text)
    units = []
    pattern = re.compile(r'(\w+)\s*:\s*(\S+)\s*\{([^}]*)\}', re.S)
    for m in pattern.finditer(text):
        typ, name, body = m.groups()
        attrs = {}
        for line in body.splitlines():
            line = line.strip().rstrip(',')
            if not line: continue
            am = re.match(r'(\w+)(\[\])?\s*:\s*"([^"]*)"', line)
            if not am: continue
            key, is_arr, val = am.groups()
            if is_arr:
                attrs.setdefault(key, []).append(val)
            else:
                attrs[key] = val
        units.append(SiiUnit(typ, name, attrs))
    return units

---

## 五、MVP 验收标准

| 功能 | 验收方式 |
|---|---|
| 扫描所有模组 | 数量与游戏内 Mod Manager 一致（你本机约 573 个） |
| 预览图展示 | 有 manifest 的 mod 能看到 276x162 缩略图 |
| 描述+版本展示 | 能看到 display_name / version / author / compatible_versions / 描述正文 |
| 批量启用/禁用 | 勾选 / 取消勾选多个 = 工具栏点"写入" = 游戏内生效 |
| 拖拽调整优先级 | 拖动行改变顺序 = 写入 = 游戏内顺序一致 |
| 预设方案 | 保存"日常跑图"预设 = 下次一键恢复同样的启用+顺序 |
| 安全备份 | 每次写入前自动备份 profile.sii，出错能手动还原 |

---

## 六、后续可选增强（非 MVP）

- [ ] 冲突检测：扫描多个 mod 的重叠文件（都有 def/vehicle/truck/... 就提示冲突风险）
- [ ] 一键推荐排序：按 category 自动套社区标准顺序
- [ ] Mod 版本检测：从 Steam Workshop API 查询订阅 mod 是否有更新
- [ ] 好友分享码：把 active_mods 列表编码为短字符串，粘贴即导入（参考 ets2-mod-swapper.vercel.app）
- [ ] 启动游戏按钮：写入后一键启动 Steam ETS2
- [ ] 暗黑模式皮肤：QSS 样式表复刻游戏内深色主题

---

&gt; 下一步建议：如果你认同 Python + PySide6 方案，告诉我"开工"，我就直接在 F:\ETS2ModManager\ 下创建完整项目骨架 + 阶段 1 核心代码（sii_parser / scs_archive / mod_scanner），让你当天就能在控制台里看到解析出的 573 个模组列表！